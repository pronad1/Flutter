import 'package:flutter/cupertino.dart';

class PhoneFragment extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Container(
      child: Center(
        child: Text("PhoneFragment"),
      ),
    );
  }
}